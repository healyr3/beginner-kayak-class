# Beginner Kayak Class 2 - Environment Configuration
# This is a static site deployment configuration

# Server Settings
SERVER_PORT=8080

# JVM Settings (adjust based on your environment)
JAVA_TOOL_OPTIONS=-Xmx256m -Xms128m

# Spring Profile
SPRING_PROFILES_ACTIVE=development

# Instructions:
# 1. Copy this file to .env (local development only)
# 2. Adjust JVM memory settings if needed
# 3. Never commit .env to version control (it's in .gitignore)
# 4. For production, set these environment variables in your deployment platform
